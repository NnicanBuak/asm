#!/bin/bash

if [ $# -lt 1 ]; then
    echo "Usage: asm-build <file.asm>"
    exit 1
fi

source=""
if [ ! -f "" ]; then
    echo "File not found: " >&2
    exit 1
fi

base=""
obj=".o"
out=""

if nasm -f elf64 "" -o ""; then
    ld "" -o ""
fi